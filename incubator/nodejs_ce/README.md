This runtime is a fork of the stable/nodejs.

It includes support for CloudEvents 0.1. The goal of this runtime is to be eventually merged with the stable when all the languages support the new standard.
